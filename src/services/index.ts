/**
 * Services layer — external service abstractions.
 *
 * Future services:
 * - SaveGameService — local storage / cloud save
 * - LeaderboardService — online leaderboards
 * - AnalyticsService — gameplay analytics
 * - AssetService — asset loading and caching
 */

export {};
